# main.py

"""
Blank template for a Cogniconsole node.

Each node must expose a single entry point: `run(msg, bot)`.
- msg: the message or input received from the previous node
- bot: the Bot instance controlling execution, memory, and node navigation

Developers may add any additional functions, classes, or logic inside this file.
"""

def run(bot, msg):
    """
    Entry point for this node.
    
    Parameters:
    - msg: user message or input from previous node
    - bot: Bot instance providing access to short/long memory, LLM queries, 
      and node navigation (bot.next_node())
    """
    # TODO: implement node logic here
    pass